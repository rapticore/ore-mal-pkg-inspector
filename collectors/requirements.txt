# Python dependencies for OreNPMGuard Dynamic Package Collectors
# Install with: pip install -r requirements.txt

# YAML configuration parsing
PyYAML>=6.0

# Version parsing and comparison
packaging>=21.0

# Optional: For improved HTTP requests (fallback to urllib if not available)
# requests>=2.31.0

# Optional: For web scraping Phylum blog (fallback to simple parsing if not available)
# beautifulsoup4>=4.12.0
